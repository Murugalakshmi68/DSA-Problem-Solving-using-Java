class Solution {
    int missingNum(int arr[]) {
        // code here
        
        int n = arr.length + 1;

        int totalSum = n * (n + 1) / 2;
        int arraySum = 0;

        for (int x : arr) {
            arraySum += x;
        }

        return totalSum - arraySum;
    }
}
